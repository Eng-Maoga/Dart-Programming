void main() {
  int age = 18;
  assert(age >= 18, "Age must be at least 18");  // No output if true
  print("You are $age years old.");
}
// The assert statement is used for debugging purposes to check for conditions that should always be true.